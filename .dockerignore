# 依赖目录(容器内重新安装,不复制宿主的)
**/node_modules

# 构建产物(容器内重新构建)
**/.next
**/out
**/dist
**/build
**/.turbo

# 版本控制
.git
.gitignore
.gitattributes

# 环境变量(避免泄露到镜像层;运行时通过 docker env / compose 注入)
# 注意:不要用 **/.env 全局排除 + ! 例外 —— 例外规则对 frontend_uniapp/env/.env
# 未生效,导致 H5 构建缺少 VITE_APP_PROXY_ENABLE 等变量而失败。
# 改为显式排除真正含密钥的 env 文件;frontend_uniapp/env/ 下仅含客户端 VITE_ 变量,构建期必需,不排除
/.env
/.env.*
admin/.env
admin/.env.*
frontend_uniapp/.env
frontend_uniapp/.env.*

# 测试代码与产物(不进生产镜像)
admin/tests
**/coverage
**/test-results
**/playwright-report
**/.nyc_output

# IDE / 工具本地状态
.trae
.pai
.vscode
.idea
**/*.swp
**/*.swo

# 文档(非运行时必需)
docs
**/*.md

# Docker 自身(避免进入构建上下文)
Dockerfile
.dockerignore
**/docker-compose*.yml
# nginx.conf 为宿主机/网关部署参考配置,不进镜像
nginx.conf

# 日志与临时文件
**/*.log
**/*.tmp
**/.DS_Store
**/*.tsbuildinfo
**/next-env.d.ts

# Taro / 小程序本地缓存
**/.swc
**/.taro
