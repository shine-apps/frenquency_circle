# frontend 环境变量说明
#
# 自本版本起,frontend 不再读取 .env 在构建期注入运行时配置。
# 高德地图 key 等运行时配置改由 admin 后端通过 GET /api/config/amap.js
# 端点动态注入到 H5 浏览器(见 frontend/src/index.html 与 admin/.env)。
#
# 本地开发:
#   1. 在 admin/.env 中设置 AMAP_KEY / AMAP_SECURITY_CODE
#   2. 启动 admin 后端(npm run dev)
#   3. 启动 frontend(npm run dev:h5),H5 自动通过 webpack-dev-server
#      的 /api 代理从 admin 获取 key
#
# 因此 frontend 目录下不再需要 .env 文件,本模板仅作历史说明保留。
